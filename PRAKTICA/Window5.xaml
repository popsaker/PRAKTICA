
<Window x:Class="CapitalLeasing.Window5"
﻿<Window x:Class="PRAKTICA.Window5"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="CAPITAL LEASING - Админ панель" 
        Height="800" 
        Width="1400"
        WindowStartupLocation="CenterScreen"

        WindowState="Maximized"
        Background="#F5F5F5">

    <Window.Resources>
        <!-- Стили -->
        <Style x:Key="HeaderTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#2C3E50"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="Margin" Value="0,0,0,20"/>
        </Style>

        <Style x:Key="ButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#3498DB"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="Background" Value="#007BFF"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="5">
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center"
                                            VerticalAlignment="Center"
                                            Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#2980B9"/>
                                <Setter Property="Background" Value="#0056B3"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#004085"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DataGridStyle" TargetType="DataGrid">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#BDC3C7"/>
        <Style x:Key="DeleteButtonStyle" TargetType="Button" BasedOn="{StaticResource ButtonStyle}">
            <Setter Property="Background" Value="#DC3545"/>
            <Setter Property="Width" Value="80"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#C82333"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="EditButtonStyle" TargetType="Button" BasedOn="{StaticResource ButtonStyle}">
            <Setter Property="Background" Value="#FFC107"/>
            <Setter Property="Foreground" Value="#212529"/>
            <Setter Property="Width" Value="80"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E0A800"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="DataGridStyle" TargetType="DataGrid">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#DEE2E6"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="RowBackground" Value="White"/>
            <Setter Property="AlternatingRowBackground" Value="#F8F9FA"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="CanUserDeleteRows" Value="False"/>
            <Setter Property="CanUserReorderColumns" Value="True"/>
            <Setter Property="CanUserResizeColumns" Value="True"/>
            <Setter Property="CanUserResizeRows" Value="False"/>
            <Setter Property="SelectionMode" Value="Single"/>
            <Setter Property="SelectionUnit" Value="FullRow"/>
            <Setter Property="ScrollViewer.CanContentScroll" Value="True"/>
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto"/>
        </Style>

        <Style x:Key="DataGridColumnHeaderStyle" TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#2C3E50"/>
            <Setter Property="CanUserResizeRows" Value="False"/>
            <Setter Property="SelectionMode" Value="Single"/>
            <Setter Property="SelectionUnit" Value="FullRow"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Margin" Value="0,10,0,0"/>
        </Style>

        <Style x:Key="DataGridHeaderStyle" TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#343A40"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="BorderBrush" Value="#34495E"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>
        </Style>

        <Style x:Key="TableCellStyle" TargetType="DataGridCell">
            <Setter Property="BorderBrush" Value="#ECF0F1"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="BorderThickness" Value="0,0,1,0"/>
            <Setter Property="BorderBrush" Value="#495057"/>
        </Style>

        <Style x:Key="PanelContainerStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="BorderBrush" Value="#DEE2E6"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Margin" Value="20"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Шапка -->
        <Border Grid.Row="0" Background="#2C3E50" Padding="20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <TextBlock Text="🔧 " FontSize="28" VerticalAlignment="Center" Foreground="White"/>
                    <TextBlock Text="CAPITAL LEASING" 
                             FontSize="24" 
                             FontWeight="Bold" 
                             Foreground="White"
                             VerticalAlignment="Center"/>
                </StackPanel>

                <TextBlock Grid.Column="1" 
                         Text="Админ панель" 
                         FontSize="20" 
                         Foreground="White"
                         HorizontalAlignment="Center"
                         VerticalAlignment="Center"/>

                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <Button x:Name="LogoutButton" 
                          Content="Выйти" 
                          Background="#E74C3C"
                          Foreground="White"
                          Padding="15,8"
                          Cursor="Hand"
                          BorderThickness="0">
                        <Button.Triggers>
                            <EventTrigger RoutedEvent="MouseEnter">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <ColorAnimation To="#C0392B" 
                                                      Duration="0:0:0.2"
                                                      Storyboard.TargetProperty="(Button.Background).(SolidColorBrush.Color)"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </Button.Triggers>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Основное содержание -->
        <Grid Grid.Row="1" Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Панель управления -->
            <Border Grid.Row="0" 
                  Background="White" 
                  CornerRadius="10" 
                  Padding="20" 
                  Margin="0,0,0,20"
                  BorderBrush="#ECF0F1"
                  BorderThickness="1">
                <StackPanel>
                    <TextBlock Text="Панель товаров на сайте" 
                             Style="{StaticResource HeaderTextStyle}"/>

                    <WrapPanel>
                        <Button x:Name="AddProductButton" 
                              Style="{StaticResource ButtonStyle}"
                              Content="➕ Добавить товар"/>

                        <Button x:Name="EditProductButton" 
                              Style="{StaticResource ButtonStyle}"
                              Content="✏️ Редактировать"/>

                        <Button x:Name="DeleteProductButton" 
                              Style="{StaticResource ButtonStyle}"
                              Background="#E74C3C"
                              Content="🗑️ Удалить">
                            <Button.Triggers>
                                <EventTrigger RoutedEvent="MouseEnter">
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <ColorAnimation To="#C0392B" 
                                                          Duration="0:0:0.2"
                                                          Storyboard.TargetProperty="(Button.Background).(SolidColorBrush.Color)"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </EventTrigger>
                            </Button.Triggers>
                        </Button>

                        <Button x:Name="RefreshButton" 
                              Style="{StaticResource ButtonStyle}"
                              Content="🔄 Обновить"/>
                    </WrapPanel>
                </StackPanel>
            </Border>

            <!-- Таблица товаров -->
            <Border Grid.Row="1" 
                  Background="White" 
                  CornerRadius="10"
                  BorderBrush="#ECF0F1"
                  BorderThickness="1"
                  Padding="10">
                <DataGrid x:Name="ProductsDataGrid"
                        Style="{StaticResource DataGridStyle}"
                        ColumnHeaderStyle="{StaticResource DataGridColumnHeaderStyle}"
                        CellStyle="{StaticResource TableCellStyle}"
                        ItemsSource="{Binding}">

                    <DataGrid.Columns>
                        <!-- ID (скрытый) -->
                        <DataGridTextColumn Header="ID" 
                                          Binding="{Binding Id}" 
                                          Width="50" 
                                          Visibility="Collapsed"/>

                        <DataGridTextColumn Header="Название" 
                                          Binding="{Binding Name}" 
                                          Width="150"/>

                        <DataGridTextColumn Header="Цена" 
                                          Binding="{Binding Price, StringFormat={}{0:C}}" 
                                          Width="100"/>

                        <DataGridTextColumn Header="Ежимесячный платеж" 
                                          Binding="{Binding YellowField}" 
                                          Width="140"/>

                        <DataGridTextColumn Header="Мощность" 
                                          Binding="{Binding Quantity}" 
                                          Width="100"/>

                        <DataGridTextColumn Header="КПП" 
                                          Binding="{Binding ITP}" 
                                          Width="70"/>

                        <DataGridTextColumn Header="Цвет кузова" 
                                          Binding="{Binding CVKU}" 
                                          Width="90"/>

                        <DataGridTextColumn Header="Год" 
                                          Binding="{Binding Year}" 
                                          Width="50"/>

                        <DataGridTextColumn Header="Расположение руля" 
                                          Binding="{Binding Expendable}" 
                                          Width="150"/>

                        <DataGridTextColumn Header="Пробег" 
                                          Binding="{Binding Limit}" 
                                          Width="80"/>

                        <DataGridTextColumn Header="Количество ключей" 
                                          Binding="{Binding ServiceSigns}" 
                                          Width="150"/>

                        <DataGridTextColumn Header="Город" 
                                          Binding="{Binding City}" 
                                          Width="120"/>

                        <DataGridTextColumn Header="Рабочий объем" 
                                          Binding="{Binding BothExpenses}" 
                                          Width="140"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>
        </Grid>

        <!-- Статус бар -->
        <StatusBar Grid.Row="2" Background="#34495E">
            <StatusBarItem>
                <TextBlock x:Name="StatusText" 
                         Foreground="White" 
                         Margin="10,0">
                    Готово
                </TextBlock>
            </StatusBarItem>
            <StatusBarItem HorizontalAlignment="Right">
                <TextBlock x:Name="RowCountText" 
                         Foreground="White" 
                         Margin="10,0">
                    Товаров: 0
                </TextBlock>
            </StatusBarItem>
        </StatusBar>

        </Grid.RowDefinitions>

        <!-- Шапка -->
        <Border Background="#343A40" Padding="20">
            <DockPanel LastChildFill="False">
                <StackPanel DockPanel.Dock="Left">
                    <TextBlock Text="CAPITAL LEASING" 
                             FontSize="28" 
                             FontWeight="Bold" 
                             Foreground="White"/>
                    <TextBlock Text="Админ панель" 
                             FontSize="18" 
                             Foreground="#ADB5BD"
                             Margin="0,5,0,0"/>
                </StackPanel>

                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                    <Button Content="Добавить товар" 
                          Style="{StaticResource ButtonStyle}"
                          Click="AddCar_Click"
                          Margin="0,0,10,0"/>
                    <Button Content="Обновить" 
                          Style="{StaticResource ButtonStyle}"
                          Click="RefreshData_Click"
                          Margin="0,0,10,0"/>
                    <Button Content="Выйти" 
                          Style="{StaticResource ButtonStyle}"
                          Background="#6C757D"/>
                </StackPanel>
            </DockPanel>
        </Border>

        <!-- Основное содержимое -->
        <Border Grid.Row="1">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <!-- Панель фильтров и поиска -->
                    <Border Style="{StaticResource PanelContainerStyle}">
                        <StackPanel>
                            <TextBlock Text="Панель товаров на сайте" 
                                     Style="{StaticResource HeaderTextStyle}"/>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <!-- Исправлено: убран PlaceholderText, добавлен Watermark через привязку -->
                                <TextBox x:Name="SearchTextBox" 
                                       FontSize="14"
                                       Padding="12"
                                       Margin="0,0,10,0">
                                    <TextBox.Style>
                                        <Style TargetType="TextBox">
                                            <Setter Property="Background" Value="White"/>
                                            <Setter Property="BorderBrush" Value="#CED4DA"/>
                                            <Setter Property="BorderThickness" Value="1"/>
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="TextBox">
                                                        <Grid>
                                                            <Border x:Name="border"
                                                                    BorderBrush="{TemplateBinding BorderBrush}"
                                                                    BorderThickness="{TemplateBinding BorderThickness}"
                                                                    Background="{TemplateBinding Background}"
                                                                    CornerRadius="4">
                                                                <ScrollViewer x:Name="PART_ContentHost"
                                                                            Margin="{TemplateBinding Padding}"/>
                                                            </Border>
                                                            <TextBlock x:Name="placeholderText"
                                                                     Text="Поиск по названию..."
                                                                     Foreground="Gray"
                                                                     Margin="{TemplateBinding Padding}"
                                                                     VerticalAlignment="Center"
                                                                     IsHitTestVisible="False">
                                                                <TextBlock.Style>
                                                                    <Style TargetType="TextBlock">
                                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                                        <Style.Triggers>
                                                                            <DataTrigger Binding="{Binding Text, RelativeSource={RelativeSource TemplatedParent}}" Value="">
                                                                                <Setter Property="Visibility" Value="Visible"/>
                                                                            </DataTrigger>
                                                                        </Style.Triggers>
                                                                    </Style>
                                                                </TextBlock.Style>
                                                            </TextBlock>
                                                        </Grid>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsFocused" Value="True">
                                                                <Setter TargetName="border" Property="BorderBrush" Value="#007BFF"/>
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </TextBox.Style>
                                </TextBox>

                                <Button Grid.Column="1" 
                                      Content="Поиск" 
                                      Style="{StaticResource ButtonStyle}"/>                            </Grid>
                        </StackPanel>
                    </Border>

                    <!-- Таблица товаров -->
                    <Border Style="{StaticResource PanelContainerStyle}">
                        <StackPanel>
                            <DataGrid x:Name="CarsDataGrid"
                                    Style="{StaticResource DataGridStyle}"
                                    ColumnHeaderStyle="{StaticResource DataGridHeaderStyle}"
                                    LoadingRow="CarsDataGrid_LoadingRow">

                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Название" 
                                                      Binding="{Binding Name}" 
                                                      Width="150"/>
                                    <DataGridTextColumn Header="Цена" 
                                                      Binding="{Binding Price, StringFormat={}{0:N0} ₽}" 
                                                      Width="100"/>
                                    <DataGridTextColumn Header="Количество" 
                                                      Binding="{Binding Quantity}" 
                                                      Width="100"/>
                                    <DataGridTextColumn Header="Мощность" 
                                                      Binding="{Binding Power, StringFormat={}{0} л.с.}" 
                                                      Width="100"/>
                                    <DataGridTextColumn Header="КПП" 
                                                      Binding="{Binding Transmission}" 
                                                      Width="80"/>
                                    <DataGridTextColumn Header="Цвет" 
                                                      Binding="{Binding Color}" 
                                                      Width="100"/>
                                    <DataGridTextColumn Header="Год" 
                                                      Binding="{Binding Year}" 
                                                      Width="80"/>
                                    <DataGridTextColumn Header="Руль" 
                                                      Binding="{Binding SteeringWheel}" 
                                                      Width="100"/>
                                    <DataGridTextColumn Header="Пробег" 
                                                      Binding="{Binding Mileage, StringFormat={}{0:N0} км}" 
                                                      Width="100"/>
                                    <DataGridTextColumn Header="Ключи" 
                                                      Binding="{Binding Keys}" 
                                                      Width="100"/>
                                    <DataGridTextColumn Header="Город" 
                                                      Binding="{Binding City}" 
                                                      Width="100"/>
                                    <DataGridTextColumn Header="Объем" 
                                                      Binding="{Binding EngineVolume, StringFormat={}{0} л}" 
                                                      Width="80"/>

                                    <DataGridTemplateColumn Header="Действия" Width="180">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <StackPanel Orientation="Horizontal">
                                                    <Button Content="Редактировать" 
                                                          Style="{StaticResource EditButtonStyle}"
                                                          Click="EditCar_Click"
                                                          Tag="{Binding Id}"/>
                                                    <Button Content="Удалить" 
                                                          Style="{StaticResource DeleteButtonStyle}"
                                                          Click="DeleteCar_Click"
                                                          Tag="{Binding Id}"/>
                                                </StackPanel>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>
                                </DataGrid.Columns>
                            </DataGrid>

                            <!-- Статистика -->
                            <Border Background="#F8F9FA" 
                                  CornerRadius="6" 
                                  Padding="15" 
                                  Margin="0,20,0,0">
                                <DockPanel>
                                    <TextBlock Text="{Binding ElementName=CarsDataGrid, Path=Items.Count, StringFormat='Всего товаров: {0}'}"
                                           FontWeight="SemiBold"/>
                                    <TextBlock x:Name="TotalValueText"
                                           DockPanel.Dock="Right"
                                           FontWeight="SemiBold"
                                           Foreground="#007BFF"/>
                                </DockPanel>
                            </Border>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </ScrollViewer>
        </Border>
    </Grid>
</Window>
